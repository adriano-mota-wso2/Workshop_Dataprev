openapi: 3.0.1
info:
  title: HondaVehicles
  description: API Definition of HondaVehicles
  version: 1.0.0
servers:
- url: http://localhost:8290/hondavehicles
paths:
  /list:
    get:
      parameters:
      - name: type
        in: query
        schema:
          type: string
      responses:
        default:
          description: Default response
