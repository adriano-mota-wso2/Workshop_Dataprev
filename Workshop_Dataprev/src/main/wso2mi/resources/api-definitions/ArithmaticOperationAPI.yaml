openapi: 3.0.1
info:
  title: ArithmaticOperationAPI
  description: API Definition of ArithmaticOperationAPI
  version: 1.0.0
servers:
- url: http://localhost:8290/arithmaticoperationapi
paths:
  /:
    post:
      requestBody:
        description: Sample Payload
        content:
          application/json:
            schema:
              type: object
              properties:
                payload:
                  type: object
        required: false
      responses:
        default:
          description: Default response
